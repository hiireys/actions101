name: CI/CD Pipeline
on: 
  push:
    branches: 
       - main
       - feature/*
       - release/*
    tags: 
       - release-*


jobs:
  buildJob:
    runs-on: [ self-hosted ]
    steps: 
        - name: Checkout code
          uses: actions/checkout@v4

        - name: Run ls command to list files
          run: |
            ls
            pwd
            whoami
            uname -a
