name: CI/CD Pipeline - FEATURE
on: 
  push:
    branches: 
       - main
       - feature/*
       - release/*
    tags: 
       - release-*
  workflow_dispatch:

jobs:
  devBuildJob:
    runs-on: [ self-hosted ]
    environment: Development
    steps: 
       - name: Run command 
         run: | 
           echo "Hello from dev job"
  